node_modules/
.next/
out/
build/
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
scratch/
_*.mjs

# runbook (.claude/skills/) agora é versionado — backup junto do código.
# As senhas das contas de teste ficam em arquivos *.local.md, que continuam
# só nesta máquina; o lock de tarefas agendadas é runtime, não conteúdo.
*.local.md
.claude/scheduled_tasks.lock

# planilha de trabalho solta na raiz — nao pertence ao repo
Alimentos_POF_x_App.xlsx

# Diretorio temporario do CLI do Supabase
supabase/.temp/
